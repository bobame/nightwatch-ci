language: node_js
node_js:
  - "8"
services:
  - xvfb
dist: trusty
addons:
  chrome: stable
before_install:
  - google-chrome-stable --headless --disable-gpu --remote-debugging-port=9922 http://localhost &
script:
  - xvfb-run npm test
